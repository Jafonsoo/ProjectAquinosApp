<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="M2UApp.Views.SignUpPage"
             Title="Página de Registo">
    <ContentPage.Content>
        <StackLayout Padding="20" VerticalOptions="Center">
            <Entry x:Name="Emailentery" Placeholder="Email" Text="{Binding Email}" Keyboard="Email"
                 />
            <Entry x:Name="passwordentery" Placeholder="Password" Text="{Binding Password}" 
                   IsPassword="True"/>
            <Entry x:Name="cfmpasswordentery" Placeholder="Confirmar Password" Text="{Binding ConfirmPassword}" 
                   IsPassword="True" />
            <Button x:Name="signup" Text="SignUp"
                    Command="{Binding SignUpCommand}"
                    HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>